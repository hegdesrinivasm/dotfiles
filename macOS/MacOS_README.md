# macOS dotfiles
To run ```setup_mac.sh```, run the following commands in succession:
```chmod +x setup_mac.sh```
```./setup_mac.sh```

To run ```gitConfig.zsh```, run the following commands in succession:
```chmod +x gitConfig.zsh```
```./gitConfig.zsh```
